lado = float(input("Introduzca la extensión del lado del cubo: "))
volumen = round(pow(lado,3),4)
area = round(pow(lado,2)*6,4)
print(f"El área y el volumen de un cubo, cuyo lado es {lado}, viene dado por {area} y {volumen}, respectivamente")
